# 安全策略

## 当前支持状态

当前维护版本为 v1.0.0。安全修复优先合入最新版本；旧版本是否回补将在对应 Release 说明中注明。

## 报告漏洞

不要在公开 Issue 中披露漏洞、凭据、内部地址或真实数据。请使用仓库维护者公布的私密安全渠道，并提供影响范围、复现条件和修复建议。仓库公开前应在 GitHub 启用私密漏洞报告。

## 部署要求

- 更换默认 `admin/admin`；
- 为 `JWT_SECRET` 使用至少 32 字节的随机值；
- 更换 MinIO 访问密钥并限制网络暴露；
- 只通过环境变量或密钥系统注入凭据；
- 为 Web 与对象存储配置 HTTPS；
- 限制 MongoDB、Redis、API 和 MinIO 控制台的公网访问；
- 定期备份 MongoDB 和 MinIO，并验证恢复；
- 更新基础镜像并执行依赖、镜像和密钥扫描。

日志与审计不得保存密码、令牌、预签名 URL 或文件正文。公开测试数据必须为合成数据。

## 已知安全边界

当前版本面向可信内网团队，采用单机部署，不包含多租户隔离、高可用和外部身份提供商。CVAT 和 MLflow 在线凭据配置尚未启用。

远程 Git 推送只能在项目所有者手工测试并明确授权后执行。
