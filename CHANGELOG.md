# 变更日志

本项目遵循 Keep a Changelog，正式版本使用语义化版本。

## [Unreleased]

## [1.2.1] - 2026-09-23

- 64 MB 及以上文件改为 64 MB 分片上传；重新选择同一文件时复用未过期会话并跳过已完成分片，完成后由 MinIO 合并并清理临时分片。
- 素材库“全部”采用服务端选择集，批量标签、删除、集合创建和导出支持选择集及手动排除项；全部模式改为分批继续加载，避免一次读取所有卡片。
- 导出接口不再接受原始数据库查询对象，只接受显式素材 ID 或服务端选择集；选择集导出不再把大量素材 ID 写入任务记录。

### Changed

- 多文件上传合并创建和完成请求，并提高小文件并发数；素材“全部”读取改用连续分页游标。
- 数据集列表批量统计成员构成，数据集写入锁支持过期续租；中断的版本恢复可在下一次写入前补完。
- Docker Compose 的 Worker 使用可配置 MinIO 凭据，浏览器访问地址及 HTTPS 可单独配置。
- 更新存在安全公告的 Python 依赖，CI 增加依赖与容器镜像扫描。

### Added

- Docker Compose 备份与新项目恢复脚本，以及隔离数据库的合成素材性能基线脚本。

## [1.2.0] - 2026-09-21

### Changed

- 训练溯源收敛为“图片 → 标注”和“素材 → 数据集版本 → 模型”；拒绝新建模型直连素材关系，版本成员路径从快照推导。
- 数据集手动成员不再接受标注；图片加入、移出数据集或解除标注关系时，同步维护当前数据集的关联标注，已发布版本保持不变。
- 数据集添加素材和筛选弹窗统一类型、标签与时间筛选，修正视频加入、成员分页和选择状态。
- 模型溯源按版本成员展示图片、标注、视频、压缩文件及其他素材，空类别隐藏，并标出每项素材来源的数据集和版本。
- 多文件上传调整并发数量并显示已处理进度；无类别表的 YOLO 标注显示“未命名类别”。

### Added

- 旧模型直连关系的只读迁移报告和显式备份清理脚本。
- 数据集图片与关联标注的成员预览、同步和回归测试。

### Known limitations

- 上传仍为整文件预签名传输，不支持分片、暂停或刷新恢复。
- 大批量真实素材的提速幅度、10 万条性能、全新 Linux 部署与完整故障恢复尚未验收。

## [1.1.0] - 2026-09-16

### Added

- 数据集新建、编辑、删除、两种复制范围与自定义副本名称。
- 数据管理分页、类型与高级筛选、跨页保留选择和批量成员操作。
- 不可变版本发布、自定义版本号、版本详情、差异对比和恢复。
- 数据集版本与模型关联、关联备注、关联素材关系的批量清除。
- 素材详情展示所属数据集，支持跳转数据管理。
- 数据集异步导出，以及全部成员的标签分布统计。

### Changed

- 删除数据集同步清除历史版本、版本成员和对应模型关系，保留素材文件。
- 已加入数据集的素材禁止从素材库删除。
- 模型溯源按当前模型展示素材汇总和数据集版本。
- 数据集分页总数、筛选应用时机、时间范围控件和复制弹窗统一调整。
- 批量删除相同失败原因合并展示。
- 数据集写入增加并发限制与捕获异常后的失败回滚。

### Known limitations

- 上传仍使用整文件预签名上传，暂不支持分片、暂停及刷新恢复。
- “全部”分批展示，但仍会加载全部数据到内存；尚未完成10万条性能验收。
- 强制终止服务后的锁恢复和操作恢复仍需完善。
- 重复添加同一模型版本的关联分组处理仍有已知边界，详见发布说明。

## [1.0.0] - 2026-09-14

### Added

- 方案 A“数据透镜”界面、白天模式和夜间模式；
- 登录、可撤销刷新令牌、五类角色和用户管理；
- 多文件预签名上传、格式识别、10 GB 可配置单文件限制；
- 图片与视频预览、原分辨率查看、视频播放；
- 名称与备注模糊搜索、高级筛选、保存视图和多值标签；
- YOLO、Pascal VOC、CVAT Images、COCO 图片标注包导入；
- 矩形框和多边形标注叠加；
- 图片与标注自动关联、模型批量关联、关系历史、软撤销和模型溯源；
- 回收站还原与后台物理清理；
- 异步 ZIP 导出、限时下载和失败重试；
- 标签、格式、角色、审计和存储空间管理；
- Windows 本机一键环境、Docker Compose 和离线镜像脚本。

### Changed

- “图片+标注”改为手动选择的导入方式，成功后移除原压缩资产；
- 导出 ZIP 只保留所选原文件；
- 关系撤销原因改为选填；
- 关系选择器禁止选择源对象自身；
- 成功和错误消息分别展示，错误消息不再带成功勾号。

### Security

- 原始规划文件、真实素材、模型权重、运行数据和环境密钥均从公开仓库排除。
