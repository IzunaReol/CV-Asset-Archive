# MongoDB 迁移规范

数据库结构变化通过 `backend/app/migrations.py` 中的顺序迁移执行。每个迁移拥有不可变整数版本和名称，成功后写入 `schema_migrations`。迁移必须可重复运行，避免依赖某次部署的内存状态。

新增迁移时不得修改已经发布的迁移函数。涉及大量数据的迁移应采用分批游标并在部署说明中标注预计时间、磁盘需求和恢复方式。应用启动前应完成 MongoDB 与 MinIO 备份。
